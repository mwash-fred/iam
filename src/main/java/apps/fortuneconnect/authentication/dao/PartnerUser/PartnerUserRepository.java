package apps.fortuneconnect.authentication.dao.PartnerUser;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PartnerUserRepository extends JpaRepository<PartnerUser, String> {
}
